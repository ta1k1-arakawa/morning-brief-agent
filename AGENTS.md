# Codex 作業指示

このリポジトリで作業を始める前に、必ず `PROJECT_CONTEXT.md` を読んでください。

## 基本方針

- `PROJECT_CONTEXT.md` に書かれている目的、MVP、作業方針、実装順序に従う。
- `tests/` フォルダは作らない。
- 1ファイルずつ実装する。
- 1ファイル作ったらそこで止まり、ユーザーにコードを分かりやすく解説する。
- ユーザーが確認してから次のファイルへ進む。
- 秘密情報やAPIキーをコードに直接書かない。
- `.env` はローカル開発用として使い、Git 管理しない。

## 再開ルール

作業を再開するときは、`PROJECT_CONTEXT.md` の「実装順序と再開ルール」を確認してください。

基本的には次の手順で進めます。

1. 実装順序を上から確認する。
2. すでに存在するファイルはスキップする。
3. 最初に存在しないファイルを、その日の開始地点にする。
4. ファイルが存在していても未完成そうなら、中身を読んで続きから直す。
5. 1ファイル作ったら止まり、ユーザーに一から分かりやすく解説する。

## 説明スタイル

- 初心者にも分かるように説明する。
- クラス、インスタンス、`self`、`dataclass`、`Protocol` などは、必要に応じて噛み砕いて説明する。
- 何のためのファイルか、どの処理につながるかを最初に説明する。
- コードの細かい意味も、ユーザーが求めたら1行ずつ説明する。
